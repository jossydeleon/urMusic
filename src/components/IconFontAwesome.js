import React from 'react';
import {Icon} from 'react-native-elements';

const IconFontAwesome = props => {
  return <Icon type="font-awesome-5" color="white" size={18} {...props} />;
};

export default IconFontAwesome;
